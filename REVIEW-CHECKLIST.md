# 反向验证清单

第三轮核对的操作参考。对每条「✓ 已确认」条目执行以下六项检查。

---

## 六项检查

### ① 上下文语义一致性

匹配到的原文片段，是否真的在说同一件事？

- ✅ 通过：原文与目标表述指向同一概念
- ❌ 误判：关键词仅碰巧出现在无关上下文中

### ② 专有名词精确性

目标文档中的项目/机构/平台名称，与参考文档中的名称**逐字比对**。

- ✅ 通过：名称完全一致
- ❌ 不一致：存在任何差异

### ③ 数值精确性

目标文档中的**每一个**数字逐项核对是否在参考文档中有完全一致的出处。

- ✅ 通过：所有数字与参考文档完全一致
- ⚠️ 部分：部分数字有出处、部分缺失
- ❌ 不一致：数字不一致

### ④ 增长率/派生数据独立验证

基数匹配 ≠ 增长率匹配。增长率必须在参考文档中**独立找到**。

- ✅ 通过：增长率数字在参考文档中有对应的增长语境
- ❌ 不一致：增长率数字存在但值不同
- ❌ 缺失：增长率数字在参考文档中完全不存在

### ⑤ 复合数据完整性

当表述包含多个并列子项（"A、B和C"）时，每个子项是否都有独立出处？

- ✅ 通过：所有子项均可独立验证
- ⚠️ 部分：部分子项有出处、部分无

### ⑥ 口径一致性

统计范围、单位和分级口径是否一致？

- 如"AA类及以上" ≠ "AA类"
- 如"到账经费增长70.4%" ≠ "科研事业收入4.43亿"
- 如"教学育人平台" ≠ "科研平台"

---

## 检查结果处理

| 修改后状态 | 触发条件 |
|-----------|---------|
| 保持「✓ 已确认」 | 六项检查全部通过 |
| 「△ 数据不一致」 | 检查③/④/⑤/⑥ 发现数据差异 |
| 「△ 部分匹配」 | 数据有出处但不完整或含概括性内容 |
| 「✗ 未找到」 | 检查① 发现完全误判，实际无对应出处 |

---

## 最危险信号

| 信号 | 说明 |
|------|------|
| 数字搜索返回 0 结果但已标 ✓ | 极可能误匹配，关键词碰巧命中文字而非数字 |
| 关键词过于通用 | "平台""建设""两重"等 → 必须检查上下文 |
| 表述含 2+ 数字 | 每个数字都要独立验证 |
| 表述含增长率/增幅 | 增长率必须单独找到，基数对 ≠ 增长率对 |
| 名称/项目名 | 与参考文档逐字比对 |
| 表述含多个并列实体 | "A入选X，获批B"→ 可能仅B匹配，须拆开分别验证 |
| 实体-语境不匹配（自动标记） | 实体在参考文档存在但从不与匹配关键词共现 → 张冠李戴 |

---

## △ 条目复查重点

脚本已为 △ 条目算出 `反向验证警告`，先读警告再按以下三项查：

1. **子命题逐项验证**（复用检查⑤）：被标「无独立出处」的子命题是否确实无出处？
2. **专有名词精确比对**（复用检查②）：匹配出处里的实体名称与目标文档是否逐字一致？
3. **同名实体文内一致性**：同一专名在目标文档不同位置的写法是否一致？若不一致，高优先级标为待查。

复查后：确属张冠李戴/无出处 → 降为「✗ 未找到」；确为脚本误报 → 升为「✓ 已确认」。
